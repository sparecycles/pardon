config:
  auth-type: user

!!!
import { pardon } from "pardon";

export const login = pardon.flow`
    >>>
    PUT https://todo.example.com/users

    <<<
    200

    { token }
`;

>>>
ANY //
Authorization: User {{@token = login({ username, origin }).await.token}}
